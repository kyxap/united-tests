language: java

jdk:
  - oraclejdk8

before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &

dist: trusty
addons:
  chrome: stable

script: gradle test

